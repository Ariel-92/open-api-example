<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rockercats.open_api.repository.product.OpenApiExamProductMapper">
    <select id="selectProduct" parameterType="ProductDetailRequest" resultType="List">
        SELECT * FROM PRODUCT
        -- WHERE PRODUCT_ID = #{product_id}
        LIMIT 10
    </select>
</mapper>