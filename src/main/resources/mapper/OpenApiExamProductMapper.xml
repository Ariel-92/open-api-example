<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rockercats.open_api.repository.OpenApiExamProductMapper">
    <select id="selectProductList" parameterType="null" resultType="List">
        SELECT * FROM PRODUCT
        LIMIT 10
    </select>
</mapper>